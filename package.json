{
  "name": "project-pizzeria",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "license": "ISC",
  "scripts": {
    "start": "live-server src --port=5173",
    "sass": "sass src/sass:src/css --watch",
    "build:css": "sass src/sass:src/css --style=compressed",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "stylelint": "stylelint \"**/*.{css,scss}\" --allow-empty-input",
    "stylelint:fix": "stylelint \"**/*.{css,scss}\" --fix --allow-empty-input"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "live-server": "^1.2.2",
    "sass": "^1.77.0",
    "stylelint": "^16.24.0",
    "stylelint-config-standard": "^39.0.0"
  }
}
